services:
  - type: web
    name: mdownloader
    env: docker
    dockerfilePath: Dockerfile
    dockerContext: .
    plan: free
    numInstances: 1
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
